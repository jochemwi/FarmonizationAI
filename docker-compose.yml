services:
  harness:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data:ro       # raw data — read only
      - ./output:/app/output      # scripts + cleaned output — read/write